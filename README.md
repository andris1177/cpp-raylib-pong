# Használata

## Linux

cmake telepítése
```bash
sudo apt-get install cmake
```

vagy 
``` bash
sudo pacman -S cmake
```

git clone 
``` bash
git clone https://github.com/andris1177/cpp-raylib-pong.git
```

futtatás
``` bash
cd cpp-raylib-pong
mkdri build
cd build
cmake ../
make
./main
```